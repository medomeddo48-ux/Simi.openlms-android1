OpenLMS frontend packaged for Capacitor/Android.

How to use:
1. npm install
2. npm run build:web
3. npx cap add android
4. npx cap copy
5. npx cap open android
Then build in Android Studio or use Gradle: cd android && ./gradlew bundleRelease
