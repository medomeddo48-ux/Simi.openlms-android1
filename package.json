{
  "name": "openlms-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:web": "vite build",
    "preview": "vite preview",
    "cap:copy": "npx cap copy",
    "cap:add:android": "npx cap add android || true",
    "android:build:release": "npm run build:web && npx cap copy android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-router-dom": "6.14.1",
    "@capacitor/core": "5.0.0"
  },
  "devDependencies": {
    "vite": "5.1.1",
    "@vitejs/plugin-react": "4.0.0"
  }
}