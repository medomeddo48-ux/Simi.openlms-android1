import React from 'react';
export default function Home(){
  return <div style={{padding:20}}>
    <h1>OpenLMS Android</h1>
    <p>This is a packaged frontend for the OpenLMS project. It expects the backend API to be reachable at <code>/api</code> (same host).</p>
    <p>Use the menu to navigate.</p>
  </div>
}
